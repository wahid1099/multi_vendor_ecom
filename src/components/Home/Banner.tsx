// React import removed as it's not needed in modern React
import ModernBanner from "../modern/ModernBanner";

const HomeBanner = () => {
  return <ModernBanner />;
};

export default HomeBanner;
